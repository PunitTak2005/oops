#include <iostream>

int main() {
    int n;
    std::cout << "Enter the value of n: ";
    std::cin >> n;

    for (int i = 2; i <= n; i += 2) {
        std::cout << i << " ";
    }

    std::cout << std::endl;
    return 0;
}
